services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    command: ["bun", "run", "dev", "--host", "0.0.0.0"]